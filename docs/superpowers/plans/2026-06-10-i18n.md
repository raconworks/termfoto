# 中英文切换 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 添加 `L` 键即时中英文切换，集中翻译管理于 `src/lang.rs`

**Architecture:** 新建 `src/lang.rs` 模块，`Lang` 枚举提供 `detect()`/`toggle()` 及 6 个翻译方法；`App` 持有 `lang` 字段，各 UI 组件通过 `app.lang` 获取翻译字符串

**Tech Stack:** 纯 Rust，零外部依赖，`std::env::var("LANG")` 检测系统语言

---

### Task 1: `src/lang.rs` — Lang 模块

**Files:**
- Create: `src/lang.rs`

- [ ] **Step 1: 创建 Lang 枚举和 detect/toggle 方法**

```rust
/// Language for UI text. Detected from $LANG at startup, toggled by L key.
#[derive(Debug, Clone, Copy, PartialEq)]
pub enum Lang {
    Zh,
    En,
}

impl Lang {
    /// Detect language from $LANG environment variable.
    /// zh_CN, zh_TW, zh_SG, etc. → Zh; everything else → En.
    pub fn detect() -> Self {
        match std::env::var("LANG") {
            Ok(s) if s.starts_with("zh_") => Lang::Zh,
            _ => Lang::En,
        }
    }

    /// Toggle between Chinese and English.
    pub fn toggle(&mut self) {
        *self = match self {
            Lang::Zh => Lang::En,
            Lang::En => Lang::Zh,
        };
    }

    /// Browser status bar format: filename, index, total, key hints
    pub fn browser_status_fmt(&self) -> &'static str {
        match self {
            Lang::Zh => " {} [{}/{}]  ←→↑↓ 导航  PgUp/PgDown/Space翻页  Home/End首尾  Enter全屏  /搜索  q退出",
            Lang::En => " {} [{}/{}]  ←→↑↓ Nav  PgUp/PgDown/Space Page  Home/End  Enter view  /search  q quit",
        }
    }

    /// Search bar hint when there are matches
    pub fn search_hint_matches(&self) -> &'static str {
        match self {
            Lang::Zh => " [{}/{} matches]  Tab/Shift+Tab切换  Enter全屏  Esc取消",
            Lang::En => " [{}/{} matches]  Tab/Shift+Tab cycle  Enter view  Esc cancel",
        }
    }

    /// Search bar hint when query is empty
    pub fn search_hint_empty(&self) -> &'static str {
        match self {
            Lang::Zh => " Tab/Shift+Tab切换  Enter全屏  Esc取消",
            Lang::En => " Tab/Shift+Tab cycle  Enter view  Esc cancel",
        }
    }

    /// Search bar hint when no matches found
    pub fn search_hint_none(&self) -> &'static str {
        match self {
            Lang::Zh => " [0/0]  Tab/Shift+Tab切换  Enter全屏  Esc取消",
            Lang::En => " [0/0]  Tab/Shift+Tab cycle  Enter view  Esc cancel",
        }
    }

    /// Fullscreen status bar format: filename, index, total, loading suffix
    pub fn preview_status_fmt(&self) -> &'static str {
        match self {
            Lang::Zh => " {} [{}/{}]  原图尺寸  ← → 切换  Enter/Esc/q 返回{}",
            Lang::En => " {} [{}/{}]  original size  ← → prev/next  Enter/Esc/q back{}",
        }
    }

    /// Loading indicator text
    pub fn loading_text(&self) -> &'static str {
        match self {
            Lang::Zh => " ⏳ 加载中...",
            Lang::En => " ⏳ loading...",
        }
    }
}
```

- [ ] **Step 2: 编写 Lang 单元测试**

在 `src/lang.rs` 末尾追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_toggle_zh_to_en() {
        let mut lang = Lang::Zh;
        lang.toggle();
        assert_eq!(lang, Lang::En);
    }

    #[test]
    fn test_toggle_en_to_zh() {
        let mut lang = Lang::En;
        lang.toggle();
        assert_eq!(lang, Lang::Zh);
    }

    #[test]
    fn test_toggle_roundtrip() {
        let mut lang = Lang::Zh;
        lang.toggle();
        lang.toggle();
        assert_eq!(lang, Lang::Zh);
    }

    #[test]
    fn test_all_methods_return_non_empty() {
        for lang in [Lang::Zh, Lang::En] {
            assert!(!lang.browser_status_fmt().is_empty());
            assert!(!lang.search_hint_matches().is_empty());
            assert!(!lang.search_hint_empty().is_empty());
            assert!(!lang.search_hint_none().is_empty());
            assert!(!lang.preview_status_fmt().is_empty());
            assert!(!lang.loading_text().is_empty());
        }
    }

    #[test]
    fn test_zh_en_strings_differ() {
        // Each method must return different text for different languages
        assert_ne!(Lang::Zh.browser_status_fmt(), Lang::En.browser_status_fmt());
        assert_ne!(Lang::Zh.search_hint_matches(), Lang::En.search_hint_matches());
        assert_ne!(Lang::Zh.search_hint_empty(), Lang::En.search_hint_empty());
        assert_ne!(Lang::Zh.search_hint_none(), Lang::En.search_hint_none());
        assert_ne!(Lang::Zh.preview_status_fmt(), Lang::En.preview_status_fmt());
        assert_ne!(Lang::Zh.loading_text(), Lang::En.loading_text());
    }
}
```

- [ ] **Step 3: 运行测试，确认通过**

```bash
cargo test lang
```
Expected: 7 tests pass (5 new + 0 existing affected yet)

- [ ] **Step 4: 提交**

```bash
git add src/lang.rs
git commit -m "feat(lang): 新建 Lang 模块——集中翻译 + detect/toggle + 6 个翻译方法"
```

---

### Task 2: `src/main.rs` + `src/app.rs` — 接入 App

**Files:**
- Modify: `src/main.rs:1`
- Modify: `src/app.rs:21-22,42-62,196-237`

- [ ] **Step 1: main.rs 添加 mod lang 声明**

在 `src/main.rs` 第 1 行 `mod app;` 后添加：

```rust
mod lang;
```

在 `main.rs` 顶部 import 区域（`use app::...` 附近）添加：

```rust
use lang::Lang;
```

修改 `App::new(...)` 调用（第 102 行），传入 `Lang::detect()`：

```rust
let mut app = App::new(images, initial_state, load_tx, load_rx, Lang::detect());
```

- [ ] **Step 2: app.rs 添加 lang 字段**

在 `src/app.rs` 顶部 import 区域添加：

```rust
use crate::lang::Lang;
```

在 `App` struct 的 `search` 字段后添加：

```rust
    pub lang: Lang,
```

修改 `App::new()` 签名和实现，添加 `lang` 参数：

```rust
    pub fn new(
        images: Vec<ImageEntry>,
        state: AppState,
        load_tx: Sender<LoadRequest>,
        load_rx: Receiver<(usize, Protocol)>,
        lang: Lang,
    ) -> Self {
        Self {
            state,
            images,
            selected: 0,
            scroll_row: 0,
            protocol_cache: HashMap::new(),
            fullscreen_protocol: None,
            fullscreen_pending: false,
            cache_width: 0,
            visible_rows: 1,
            requested: HashSet::new(),
            search: None,
            lang,
            load_tx,
            load_rx,
        }
    }
```

- [ ] **Step 3: app.rs 添加 L 键切换**

在 `handle_key()` 中，Browser 和 Fullscreen 两个分支的 match 中各添加 `L` 键处理。

Browser 分支（在 `KeyCode::Char('q')` 之前插入）：

```rust
                    KeyCode::Char('L') | KeyCode::Char('l') => {
                        self.lang.toggle();
                    }
```

Fullscreen 分支（在 `KeyCode::Char('q')` 之前插入）：

```rust
                KeyCode::Char('L') | KeyCode::Char('l') => {
                    self.lang.toggle();
                }
```

- [ ] **Step 4: 更新 app.rs 测试中的 App::new 调用**

`make_app()` 和 `make_app_with_names()` 两个测试辅助函数中的 `App::new(...)` 需要加 `Lang::Zh` 参数：

```rust
    fn make_app(count: usize) -> App {
        let images = (0..count)
            .map(|i| ImageEntry {
                path: PathBuf::from(format!("img{:03}.png", i)),
                filename: format!("img{:03}.png", i),
            })
            .collect();
        let (tx, _rx) = std::sync::mpsc::channel::<LoadRequest>();
        let (_tx2, rx2) = std::sync::mpsc::channel::<(usize, Protocol)>();
        App::new(images, AppState::Browser, tx, rx2, Lang::Zh)
    }
```

```rust
    fn make_app_with_names(names: &[&str]) -> App {
        let images: Vec<ImageEntry> = names
            .iter()
            .map(|name| ImageEntry {
                path: PathBuf::from(name),
                filename: name.to_string(),
            })
            .collect();
        let (tx, _rx) = std::sync::mpsc::channel::<LoadRequest>();
        let (_tx2, rx2) = std::sync::mpsc::channel::<(usize, Protocol)>();
        App::new(images, AppState::Browser, tx, rx2, Lang::Zh)
    }
```

- [ ] **Step 5: 运行测试**

```bash
cargo test
```
Expected: all 46 tests pass (39 existing + 7 new lang tests)

- [ ] **Step 6: 提交**

```bash
git add src/main.rs src/app.rs
git commit -m "feat(lang): App 接入 Lang——L 键切换 + detect 自动检测"
```

---

### Task 3: `src/ui/search.rs` — SearchBar 国际化

**Files:**
- Modify: `src/ui/search.rs:147-196`

- [ ] **Step 1: SearchBar 添加 lang 字段和使用**

在 `src/ui/search.rs` 顶部添加 import：

```rust
use crate::lang::Lang;
```

修改 `SearchBar` struct：

```rust
pub struct SearchBar<'a> {
    pub state: &'a SearchState,
    pub lang: Lang,
}
```

修改 `render()` 方法中的 hint 构造逻辑（第 169-178 行），替换为（`lang` 在 `SearchBar` 上，用 `self.lang`）：

```rust
        let hint = if total_matches > 0 {
            format!(
                self.lang.search_hint_matches(),
                current, total_matches
            )
        } else if query.is_empty() {
            self.lang.search_hint_empty().to_string()
        } else {
            self.lang.search_hint_none().to_string()
        };
```

- [ ] **Step 2: 更新 SearchBar 单元测试**

修改 `test_searchbar_with_no_query` 中的 `SearchBar` 构造，添加 `lang` 字段：

```rust
    #[test]
    fn test_searchbar_with_no_query() {
        let state = SearchState::new(0, '/');
        let bar = SearchBar { state: &state, lang: Lang::Zh };
        // ... rest unchanged
    }
```

- [ ] **Step 3: 运行测试**

```bash
cargo test search
```
Expected: all search tests pass

- [ ] **Step 4: 提交**

```bash
git add src/ui/search.rs
git commit -m "feat(lang): SearchBar 使用 Lang 翻译提示文本"
```

---

### Task 4: `src/ui/browser.rs` — 浏览器状态栏国际化

**Files:**
- Modify: `src/ui/browser.rs:79-99`

- [ ] **Step 1: BrowserView 状态栏和 SearchBar 构造使用 lang**

在 `src/ui/browser.rs` 顶部 import：

```rust
use crate::lang::Lang;
```

修改 `SearchBar` 构造处（第 81 行）：

```rust
            SearchBar { state: search, lang: self.app.lang }.render(status_area, buf);
```

修改状态栏构造（第 89-96 行），用 `self.app.lang.browser_status_fmt()` 替换硬编码字符串：

```rust
            let info = format!(
                self.app.lang.browser_status_fmt(),
                selected_name,
                self.app.selected.saturating_add(1).min(self.app.images.len()),
                self.app.images.len(),
            );
```

- [ ] **Step 2: 运行测试**

```bash
cargo test
```
Expected: all tests pass

- [ ] **Step 3: 提交**

```bash
git add src/ui/browser.rs
git commit -m "feat(lang): 浏览器状态栏使用 Lang 翻译"
```

---

### Task 5: `src/ui/preview.rs` — 全屏状态栏国际化

**Files:**
- Modify: `src/ui/preview.rs:52-69`

- [ ] **Step 1: PreviewView 状态栏使用 lang**

修改状态栏和加载文本（第 53-63 行）：

```rust
            let status = if self.app.fullscreen_pending {
                self.app.lang.loading_text()
            } else {
                ""
            };
            let info = format!(
                self.app.lang.preview_status_fmt(),
                entry.filename,
                self.app.selected + 1,
                self.app.images.len(),
                status,
            );
```

- [ ] **Step 2: 编译验证**

```bash
cargo build
```
Expected: 编译通过，无警告

- [ ] **Step 3: 运行全部测试**

```bash
cargo test
```
Expected: 全部测试通过

- [ ] **Step 4: 提交**

```bash
git add src/ui/preview.rs
git commit -m "feat(lang): 全屏状态栏使用 Lang 翻译"
```

---

### 最终验证

```bash
cargo test && cargo clippy && cargo build --release
```
