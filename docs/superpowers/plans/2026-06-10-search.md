# 图片搜索功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在浏览器界面实现 vim 式增量搜索——输入即时跳转匹配项、Tab 切换、黄色高亮

**Architecture:** 新建 `src/ui/search.rs` 包含 `SearchState`（模糊匹配状态机）和 `SearchBar` widget（底部搜索栏），通过 `App.search: Option<SearchState>` 集成，`BrowserView` 在搜索模式下切换底部渲染和高亮

**Tech Stack:** Rust + ratatui 0.30 + crossterm KeyCode

---

## Task 1: `SearchState` 核心——模糊匹配与状态管理

**Files:**
- Create: `src/ui/search.rs`（`SearchState` + `SearchAction` + 匹配算法 + 单元测试）

- [ ] **Step 1: 编写空结构体和构造函数的测试**

在 `src/ui/search.rs` 末尾添加 `#[cfg(test)] mod tests`：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::scanner::ImageEntry;
    use std::path::PathBuf;

    fn make_images(names: &[&str]) -> Vec<ImageEntry> {
        names.iter().map(|name| ImageEntry {
            path: PathBuf::from(name),
            filename: name.to_string(),
        }).collect()
    }

    #[test]
    fn test_search_new_saves_selected() {
        let s = SearchState::new(5, '/');
        assert_eq!(s.saved_selected, 5);
        assert_eq!(s.trigger_char, '/');
        assert!(s.query.is_empty());
        assert!(s.matches.is_empty());
        assert_eq!(s.match_idx, 0);
    }

    #[test]
    fn test_fuzzy_match_substring() {
        let images = make_images(&["abc.png", "xyz.png", "bcd.png"]);
        let mut s = SearchState::new(0, '/');
        let action = s.handle_key(KeyCode::Char('b'), KeyModifiers::NONE, &images);
        // "b" matches "abc.png" and "bcd.png", best match depends on score
        assert!(matches!(action, SearchAction::JumpTo(_)));
        assert!(!s.matches.is_empty());
    }

    #[test]
    fn test_fuzzy_match_case_insensitive() {
        let images = make_images(&["Photo.PNG", "image.jpg"]);
        let mut s = SearchState::new(0, '/');
        let action = s.handle_key(KeyCode::Char('p'), KeyModifiers::NONE, &images);
        assert!(matches!(action, SearchAction::JumpTo(_)));
        assert!(!s.matches.is_empty());
    }

    #[test]
    fn test_fuzzy_match_no_results() {
        let images = make_images(&["abc.png", "def.jpg"]);
        let mut s = SearchState::new(0, '/');
        let action = s.handle_key(KeyCode::Char('z'), KeyModifiers::NONE, &images);
        assert!(matches!(action, SearchAction::Continue));
        assert!(s.matches.is_empty());
        assert_eq!(s.query, "z");
    }

    #[test]
    fn test_backspace_removes_char() {
        let images = make_images(&["abc.png"]);
        let mut s = SearchState::new(0, '/');
        s.handle_key(KeyCode::Char('a'), KeyModifiers::NONE, &images);
        s.handle_key(KeyCode::Char('b'), KeyModifiers::NONE, &images);
        assert_eq!(s.query, "ab");
        let action = s.handle_key(KeyCode::Backspace, KeyModifiers::NONE, &images);
        assert_eq!(s.query, "a");
        assert!(matches!(action, SearchAction::JumpTo(_)));
    }

    #[test]
    fn test_tab_cycles_forward() {
        let images = make_images(&["abc.png", "abc.jpg", "abd.png"]);
        let mut s = SearchState::new(0, '/');
        s.handle_key(KeyCode::Char('a'), KeyModifiers::NONE, &images);
        let first = s.match_idx;
        // Tab should advance to next match
        let action = s.handle_key(KeyCode::Tab, KeyModifiers::NONE, &images);
        assert!(matches!(action, SearchAction::JumpTo(_)));
        assert_eq!(s.match_idx, (first + 1) % s.matches.len());
    }

    #[test]
    fn test_backtab_cycles_backward() {
        let images = make_images(&["abc.png", "abc.jpg"]);
        let mut s = SearchState::new(0, '/');
        s.handle_key(KeyCode::Char('a'), KeyModifiers::NONE, &images);
        // Shift+Tab = BackTab
        let action = s.handle_key(KeyCode::Tab, KeyModifiers::SHIFT, &images);
        assert!(matches!(action, SearchAction::JumpTo(_)));
    }

    #[test]
    fn test_esc_cancels() {
        let mut s = SearchState::new(5, '/');
        let action = s.handle_key(KeyCode::Esc, KeyModifiers::NONE, &[]);
        assert!(matches!(action, SearchAction::Cancel));
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cargo test search -- --nocapture 2>&1 | tail -5
```
Expected: 编译失败 "failed to resolve: use of undeclared type `SearchState`"

- [ ] **Step 3: 实现 `SearchState` 结构体、`SearchAction` 枚举和模糊匹配**

在 `src/ui/search.rs` 添加：

```rust
use std::path::PathBuf;
use crossterm::event::{KeyCode, KeyModifiers};
use crate::scanner::ImageEntry;

pub struct SearchState {
    pub query: String,
    pub matches: Vec<usize>,
    pub match_idx: usize,
    pub saved_selected: usize,
    pub trigger_char: char,
}

pub enum SearchAction {
    Continue,
    JumpTo(usize),
    Cancel,
}

impl SearchState {
    pub fn new(current_selected: usize, trigger: char) -> Self {
        Self {
            query: String::new(),
            matches: Vec::new(),
            match_idx: 0,
            saved_selected: current_selected,
            trigger_char: trigger,
        }
    }

    pub fn handle_key(
        &mut self,
        code: KeyCode,
        modifiers: KeyModifiers,
        images: &[ImageEntry],
    ) -> SearchAction {
        match code {
            KeyCode::Esc => SearchAction::Cancel,
            KeyCode::Backspace => {
                self.query.pop();
                self.update_matches(images);
                self.jump_to_best()
            }
            KeyCode::Tab => {
                if modifiers.contains(KeyModifiers::SHIFT) {
                    // Shift+Tab: backward
                    if !self.matches.is_empty() {
                        let n = self.matches.len();
                        self.match_idx = (self.match_idx + n - 1) % n;
                        SearchAction::JumpTo(self.matches[self.match_idx])
                    } else {
                        SearchAction::Continue
                    }
                } else if !self.matches.is_empty() {
                    // Tab: forward
                    self.match_idx = (self.match_idx + 1) % self.matches.len();
                    SearchAction::JumpTo(self.matches[self.match_idx])
                } else {
                    SearchAction::Continue
                }
            }
            KeyCode::Char(c) => {
                self.query.push(c);
                self.update_matches(images);
                self.jump_to_best()
            }
            _ => SearchAction::Continue,
        }
    }

    fn jump_to_best(&mut self) -> SearchAction {
        if self.matches.is_empty() {
            SearchAction::Continue
        } else {
            self.match_idx = 0;
            SearchAction::JumpTo(self.matches[0])
        }
    }

    fn update_matches(&mut self, images: &[ImageEntry]) {
        let query_lower = self.query.to_lowercase();
        let mut scored: Vec<(usize, i32)> = images
            .iter()
            .enumerate()
            .filter_map(|(idx, entry)| {
                let score = fuzzy_score(&entry.filename, &query_lower)?;
                Some((idx, score))
            })
            .collect();
        scored.sort_by(|a, b| b.1.cmp(&a.1).then_with(|| a.0.cmp(&b.0)));
        self.matches = scored.into_iter().map(|(idx, _)| idx).collect();
    }
}

/// Fuzzy match: characters of `query` appear in `text` in order (case-insensitive).
/// Returns Some(score) if matched, None otherwise.
/// Higher score = better match. Consecutive chars + early position = bonus.
fn fuzzy_score(text: &str, query: &str) -> Option<i32> {
    let text_lower = text.to_lowercase();
    let text_chars: Vec<char> = text_lower.chars().collect();
    let query_chars: Vec<char> = query.chars().collect();

    if query_chars.is_empty() {
        return None;
    }

    let mut score: i32 = 0;
    let mut qi = 0; // query index
    let mut prev_pos: Option<usize> = None;

    for (ti, tc) in text_chars.iter().enumerate() {
        if qi >= query_chars.len() {
            break;
        }
        if *tc == query_chars[qi] {
            // Bonus: earlier match is better
            score += 100 - (ti as i32).min(99);

            // Bonus: consecutive match
            if let Some(prev) = prev_pos {
                if ti == prev + 1 {
                    score += 50; // consecutive bonus
                } else {
                    // Penalty for gap
                    let gap = (ti - prev) as i32;
                    score -= gap.min(10);
                }
            }

            prev_pos = Some(ti);
            qi += 1;
        }
    }

    if qi == query_chars.len() {
        Some(score)
    } else {
        None
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cargo test search::tests -- --nocapture
```
Expected: 8 tests passed

- [ ] **Step 5: Commit**

```bash
git add src/ui/search.rs Cargo.toml
git commit -m "feat(search): SearchState 核心——模糊匹配与状态管理

- SearchState: query 缓冲区、匹配列表、Tab 切换
- SearchAction: JumpTo / Continue / Cancel
- 模糊匹配算法: 大小写不敏感、顺序匹配、连续/靠前加分
- 8 个单元测试全部通过

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: `SearchBar` widget——底部搜索栏渲染

**Files:**
- Modify: `src/ui/search.rs`（追加 `SearchBar` widget + 测试）

- [ ] **Step 1: 编写 SearchBar 渲染测试**

在 `src/ui/search.rs` 的 tests 模块末尾追加：

```rust
#[test]
fn test_searchbar_with_no_query() {
    use ratatui::buffer::Buffer;
    let state = SearchState::new(0, '/');
    let bar = SearchBar { state: &state, total: 10 };
    let area = ratatui::layout::Rect { x: 0, y: 0, width: 80, height: 1 };
    let mut buf = Buffer::empty(area);
    bar.render(area, &mut buf);
    // Should render "/█" and hint text "Tab/Shift+Tab切换  Esc取消"
    let content = cell_text(&buf, area);
    assert!(content.starts_with("/"), "expected leading '/', got: {content:?}");
    assert!(content.contains("Esc"), "expected Esc hint, got: {content:?}");
}

fn cell_text(buf: &ratatui::buffer::Buffer, area: ratatui::layout::Rect) -> String {
    let mut s = String::new();
    for x in area.x..area.x + area.width {
        if let Some(cell) = buf.cell((x, area.y)) {
            s.push_str(&cell.symbol());
        }
    }
    s.trim_end().to_string()
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cargo test search::tests::test_searchbar_with_no_query
```
Expected: 编译失败 "cannot find type `SearchBar`"

- [ ] **Step 3: 实现 `SearchBar` widget**

在 `src/ui/search.rs` 中 `SearchState` impl 之后、tests 之前添加：

```rust
use ratatui::{
    buffer::Buffer,
    layout::{Alignment, Rect},
    style::{Color, Style},
    text::Span,
    widgets::{Paragraph, Widget},
};

pub struct SearchBar<'a> {
    pub state: &'a SearchState,
    pub total: usize,
}

impl<'a> Widget for SearchBar<'a> {
    fn render(self, area: Rect, buf: &mut Buffer) {
        if area.height == 0 {
            return;
        }

        let prompt = self.state.trigger_char;
        let query = &self.state.query;
        let total_matches = self.state.matches.len();
        let current = if total_matches > 0 {
            self.state.match_idx + 1
        } else {
            0
        };

        let cursor = "█";
        let display_query = format!("{}{}{}", prompt, query, cursor);

        let hint = if total_matches > 0 {
            format!(" [{}/{} matches]  Tab/Shift+Tab切换  Esc取消", current, total_matches)
        } else if query.is_empty() {
            " Tab/Shift+Tab切换  Esc取消".to_string()
        } else {
            " [0/0]  Tab/Shift+Tab切换  Esc取消".to_string()
        };

        let query_style = if total_matches == 0 && !query.is_empty() {
            Style::default().fg(Color::Red).bg(Color::DarkGray)
        } else {
            Style::default().fg(Color::White).bg(Color::DarkGray)
        };

        let hint_style = Style::default().fg(Color::Gray).bg(Color::DarkGray);

        let mut spans = Vec::new();
        spans.push(Span::styled(&display_query, query_style));
        spans.push(Span::styled(&hint, hint_style));

        Paragraph::new(ratatui::text::Line::from(spans))
            .alignment(Alignment::Left)
            .render(area, buf);
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cargo test search::tests::test_searchbar_with_no_query
```
Expected: 1 test passed

- [ ] **Step 5: Commit**

```bash
git add src/ui/search.rs
git commit -m "feat(search): SearchBar widget——底部搜索栏渲染

- prompt 字符 + query + 光标
- 匹配计数 [n/N]
- 无匹配时 query 变红
- 操作提示 Tab/Shift+Tab切换 Esc取消

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: `App` 集成——搜索状态与按键分发

**Files:**
- Modify: `src/app.rs`（新增字段 + `handle_search_key` + `handle_key` 集成分支）
- Test: `src/app.rs`（追加搜索模式测试）

- [ ] **Step 1: 编写搜索模式按键测试**

在 `src/app.rs` tests 模块中追加：

```rust
#[test]
fn test_search_triggers_on_slash() {
    let mut app = make_app(20);
    // '/' should enter search mode
    assert!(app.search.is_none());
    app.handle_key(KeyCode::Char('/'), KeyModifiers::NONE);
    assert!(app.search.is_some());
    assert_eq!(app.search.as_ref().unwrap().trigger_char, '/');
}

#[test]
fn test_search_triggers_on_backslash() {
    let mut app = make_app(20);
    app.handle_key(KeyCode::Char('\\'), KeyModifiers::NONE);
    assert!(app.search.is_some());
    assert_eq!(app.search.as_ref().unwrap().trigger_char, '\\');
}

#[test]
fn test_search_esc_exits_search() {
    let mut app = make_app(20);
    app.seleted = 10;
    app.handle_key(KeyCode::Char('/'), KeyModifiers::NONE);
    assert!(app.search.is_some());
    app.handle_key(KeyCode::Esc, KeyModifiers::NONE);
    assert!(app.search.is_none());
    // Should restore saved position
    assert_eq!(app.selected, 10);
}

#[test]
fn test_search_char_jumps_and_pushes_to_query() {
    let mut app = make_app(20);
    // All images are "img000.png".."img019.png"
    app.handle_key(KeyCode::Char('/'), KeyModifiers::NONE);
    app.handle_key(KeyCode::Char('0'), KeyModifiers::NONE);
    // Should have matched some images and jumped
    let search = app.search.as_ref().unwrap();
    assert_eq!(search.query, "0");
    assert!(!search.matches.is_empty());
}

#[test]
fn test_search_backspace_works() {
    let mut app = make_app(20);
    app.handle_key(KeyCode::Char('/'), KeyModifiers::NONE);
    app.handle_key(KeyCode::Char('x'), KeyModifiers::NONE);
    app.handle_key(KeyCode::Backspace, KeyModifiers::NONE);
    let search = app.search.as_ref().unwrap();
    assert_eq!(search.query, "");
}

#[test]
fn test_search_tab_cycles_matches() {
    // Create images with predictable matching: "a_a.png", "a_b.png", "a_c.png"
    let mut app = make_app_with_names(&["a_a.png", "a_b.png", "a_c.png", "x.png"]);
    app.handle_key(KeyCode::Char('/'), KeyModifiers::NONE);
    app.handle_key(KeyCode::Char('a'), KeyModifiers::NONE);
    let first_match_idx = app.search.as_ref().unwrap().match_idx;
    // Tab forward
    app.handle_key(KeyCode::Tab, KeyModifiers::NONE);
    let search = app.search.as_ref().unwrap();
    let expected = (first_match_idx + 1) % search.matches.len();
    assert_eq!(search.match_idx, expected);
}

fn make_app_with_names(names: &[&str]) -> App {
    let images: Vec<ImageEntry> = names.iter().map(|name| ImageEntry {
        path: PathBuf::from(name),
        filename: name.to_string(),
    }).collect();
    let (tx, _rx) = std::sync::mpsc::channel::<LoadRequest>();
    let (_tx2, rx2) = std::sync::mpsc::channel::<(usize, Protocol)>();
    App::new(images, AppState::Browser, tx, rx2)
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cargo test app::tests::test_search_triggers_on_slash
```
Expected: 编译或断言失败——`search` 字段不存在

- [ ] **Step 3: 修改 `App` 结构体和 `handle_key`**

**3a. 导入 `SearchState`**，在 `src/app.rs` 顶部添加：

```rust
use crate::ui::search::{SearchAction, SearchState};
```

**3b. 在 `App` 结构体中新增字段**（`fullscreen_pending` 之后）：

```rust
pub search: Option<SearchState>,
```

**3c. 在 `App::new` 中初始化**（`fullscreen_pending: false,` 之后）：

```rust
search: None,
```

**3d. 修改 `App::handle_key`**，在 `Browser` 分支开头插入搜索模式检测，并把 `q` 和 `Ctrl+C` 的退出检查移到搜索分支之后：

```rust
pub fn handle_key(&mut self, code: KeyCode, modifiers: KeyModifiers) -> bool {
    match self.state {
        AppState::Browser => {
            // In search mode, delegate to search handler
            if let Some(ref mut search) = self.search {
                return self.handle_search_key(search, code, modifiers);
            }

            match code {
                KeyCode::Char('q') => return true,
                KeyCode::Char('c') if modifiers.contains(KeyModifiers::CONTROL) => return true,
                KeyCode::Char('/') | KeyCode::Char('\\') => {
                    self.search = Some(SearchState::new(self.selected, match code {
                        KeyCode::Char(c) => c,
                        _ => '/',
                    }));
                    return false;
                }
                // ... 其余现有按键保持不变 ...
            }
        }
        // Fullscreen 分支不变
    }
    false
}
```

**3e. 在 `impl App` 中新增 `handle_search_key` 方法**（`handle_key` 之后）：

```rust
fn handle_search_key(
    &mut self,
    search: &mut SearchState,
    code: KeyCode,
    modifiers: KeyModifiers,
) -> bool {
    match search.handle_key(code, modifiers, &self.images) {
        SearchAction::JumpTo(idx) => {
            self.selected = idx;
            // Clamp scroll so selected is visible
            self.clamp_scroll(self.visible_rows.max(1));
            false
        }
        SearchAction::Cancel => {
            self.selected = search.saved_selected;
            self.clamp_scroll(self.visible_rows.max(1));
            self.search = None;
            false
        }
        SearchAction::Continue => false,
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cargo test app::tests::test_search_
```
Expected: 6 search tests passed（test_search_triggers_on_slash, test_search_triggers_on_backslash, test_search_esc_exits_search, test_search_char_jumps_and_pushes_to_query, test_search_backspace_works, test_search_tab_cycles_matches）

同时确保已有测试不受影响：

```bash
cargo test
```
Expected: 所有测试通过

- [ ] **Step 5: Commit**

```bash
git add src/app.rs
git commit -m "feat(search): App 集成搜索状态与按键分发

- App.search: Option<SearchState> 字段
- handle_key: 浏览器模式下 / 和 \ 触发搜索
- handle_search_key: 分发 SearchAction（JumpTo/Cancel/Continue）
- 6 个搜索模式测试 + 所有已有测试通过

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: `BrowserView` 集成——搜索栏渲染与高亮

**Files:**
- Modify: `src/ui/browser.rs`（搜索模式下底部渲染 SearchBar；匹配项边框/文件名高亮）
- Modify: `src/ui/mod.rs`（`pub mod search;`）

- [ ] **Step 1: 注册 search 模块**

在 `src/ui/mod.rs` 顶部修改：

```rust
pub mod browser;
pub mod preview;
pub mod search;
```

- [ ] **Step 2: 修改 `BrowserView::render`**

**2a. 在 `src/ui/browser.rs` 顶部导入**：

```rust
use crate::ui::search::{SearchBar, SearchState};
```

**2b. 修改状态栏部分**（`render` 方法末尾），搜索模式与非搜索模式分支：

```rust
// Status bar 部分替换为：
let search_area = Rect {
    y: area.y + grid_area.height,
    height: 1,
    ..area
};

if let Some(ref search) = self.app.search {
    SearchBar {
        state: search,
        total: self.app.images.len(),
    }.render(search_area, buf);
} else {
    let selected_name = self
        .app
        .images
        .get(self.app.selected)
        .map(|e| e.filename.as_str())
        .unwrap_or("");
    let info = format!(
        " {} [{}/{}]  ←→↑↓ 导航  PgUp/PgDown/Space翻页  Home/End首尾  Enter全屏  q退出",
        selected_name,
        self.app.selected.saturating_add(1).min(self.app.images.len()),
        self.app.images.len(),
    );
    let span = Span::styled(info, Style::default().fg(Color::White).bg(Color::DarkGray));
    Paragraph::new(span)
        .alignment(Alignment::Left)
        .render(search_area, buf);
}
```

**注意**：需要移除原来重复定义的 `status_area` / `status_height` / `search_area` 变量冲突。原代码中的 `status_area`（第 24-32 行）和 `// Status bar` 段（第 67-83 行）都需要调整为上述结构。

**2c. 修改 `is_selected` 判断**（第 56 行附近），搜索模式下当前匹配项也算选中：

```rust
let is_selected = slot == self.app.selected
    || self.app.search.as_ref().map_or(false, |s| {
        s.matches.get(s.match_idx) == Some(&slot)
    });
```

**2d. 修改高亮颜色**：搜索模式下选中项边框改用黄色

```rust
fn render_browser_cell(...)  // 中：
let border_style = if selected {
    Style::default().fg(Color::Cyan)
} else {
    Style::default().fg(Color::DarkGray)
};
// → 搜索模式下匹配项为黄色：
// 但这里 selected 已经通过 2c 扩展了，需要一个额外参数 `is_search_match` 来区分颜色
```

为简化，传入一个 `search_query: Option<&str>` 参数给 `render_browser_cell`，在函数内判断是否为搜索匹配项，边框设为黄色。

或者更简单：修改 `is_selected` 逻辑，搜索模式下选中项用黄色：

在 BrowserView::render 中，`render_browser_cell` 调用处新增参数：

```rust
let search_mode = self.app.search.is_some();
let in_matches = self.app.search.as_ref()
    .map_or(false, |s| s.matches.contains(&slot));

render_browser_cell(
    cell_area, buf,
    &self.app.images[slot].filename,
    is_selected,
    search_mode && in_matches,
    &self.app.protocol_cache,
    slot,
    self.app.search.as_ref().map(|s| s.query.as_str()),
);
```

修改 `render_browser_cell` 签名：

```rust
fn render_browser_cell(
    area: Rect,
    buf: &mut Buffer,
    filename: &str,
    selected: bool,
    search_match: bool,
    cache: &std::collections::HashMap<usize, Protocol>,
    slot: usize,
    search_query: Option<&str>,
) {
    let border_style = if selected {
        // Search match gets yellow, normal selected gets cyan
        if search_match {
            // Both selected and search match: yellow
            Style::default().fg(Color::Yellow)
        } else {
            Style::default().fg(Color::Cyan)
        }
    } else if search_match {
        // Search match but not selected: dim yellow
        Style::default().fg(Color::Rgb(128, 128, 0))
    } else {
        Style::default().fg(Color::DarkGray)
    };
    // ... 其余逻辑 ...
}
```

**2e. 文件名高亮**：在搜索模式下，对文件名中匹配的字符用黄色渲染

修改 `render_browser_cell` 中渲染文件名的部分：

```rust
// Render filename (centered, with match highlighting if in search mode)
let mut spans: Vec<Span> = Vec::new();
if let Some(query) = search_query {
    let name_lower = filename.to_lowercase();
    let query_lower = query.to_lowercase();
    let query_chars: Vec<char> = query_lower.chars().collect();
    let mut qi = 0;
    let mut chars_iter = filename.chars().peekable();
    let mut name_lower_iter = name_lower.chars().peekable();
    while let Some(ch) = chars_iter.next() {
        let name_ch_lower = name_lower_iter.next().unwrap_or(ch.to_ascii_lowercase());
        if qi < query_chars.len() && name_ch_lower == query_chars[qi] {
            spans.push(Span::styled(
                ch.to_string(),
                Style::default().fg(Color::Yellow),
            ));
            qi += 1;
        } else {
            let style = if selected {
                Style::default().fg(Color::Cyan)
            } else {
                Style::default().fg(Color::White)
            };
            spans.push(Span::styled(ch.to_string(), style));
        }
    }
} else {
    let style = if selected {
        Style::default().fg(Color::Cyan)
    } else {
        Style::default().fg(Color::White)
    };
    spans.push(Span::styled(filename, style));
}

// Center the filename spans
let total_width: usize = spans.iter().map(|s| s.width()).sum();
let name_x = name_area.x + (name_area.width.saturating_sub(total_width as u16)) / 2;
let mut x = name_x;
for span in &spans {
    let w = span.width() as u16;
    buf.set_span(x, name_area.y, span, w);
    x += w;
}
```

- [ ] **Step 3: 编译并运行测试验证**

```bash
cargo build
cargo test
```
Expected: 编译通过 + 所有测试通过（含搜索相关 14 个测试）

- [ ] **Step 4: Commit**

```bash
git add src/ui/browser.rs src/ui/mod.rs
git commit -m "feat(search): BrowserView 集成——搜索栏渲染与高亮

- 搜索模式下底部渲染 SearchBar（替代状态栏）
- 匹配项边框黄色高亮（选中匹配项亮黄，非选中暗黄）
- 文件名中匹配字符黄色高亮
- pub mod search 注册

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 端到端验证

- [ ] **Step 1: 运行全部测试**

```bash
cargo test
```
Expected: 所有测试通过

- [ ] **Step 2: 手动测试搜索功能**

```bash
cargo run -- <测试图片目录>
```

验证：
- `/` 进入搜索模式，底部显示搜索栏
- 输入字符即时跳转到匹配图片
- `Tab`/`Shift+Tab` 在匹配项间循环切换
- `Esc` 退出搜索，恢复原位置
- 无匹配时 query 变红
- 匹配项边框黄色、文件名匹配字符黄色高亮
- 搜索模式不影响非搜索的原有功能

- [ ] **Step 3: 运行 clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt -- --check
```
Expected: 零 warning，零格式差异
